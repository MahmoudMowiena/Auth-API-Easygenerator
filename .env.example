# Application environment (development | staging | production)
ENVIRONMENT=

# Application port
PORT=

# MongoDB connection string
DATABASE=

# JWT secret key (use a secure random string in production)
JWT_SECRET=

# JWT expiration (e.g., "24h", "7d", "15m")
JWT_EXPIRES_IN=

# Comma-separated list of allowed CORS origins (e.g., http://localhost:3000,http://example.com)
# Use * to allow all origins
ALLOWED_ORIGINS=